import unittest
from unnecessary_math import multiply
 
class count(unittest.TestCase):
 
    def setUp(self):
        self.string = String('text, text simple, ok, and done')
 
    def test_count(self):
        self.assertEqual(String, (30 TEXT 5))
 
if __name__ == '__main__':
    unittest.main()
